{
  "name": "bulk-email-marketing-ui",
  "version": "1.0.0",
  "description": "Bulk email sending marketing UI",
  "main": "app.py",
  "scripts": {
    "dev": "set PYTHONIOENCODING=utf-8 && set FLASK_APP=app.py && python -m flask run --port=5001",
    "start": "set PYTHONIOENCODING=utf-8 && set FLASK_APP=app.py && python -m flask run --port=5001",
    "install-py": "pip install -r requirements.txt"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
